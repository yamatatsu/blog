<!DOCTYPE html>
<html ⚡>
  <%- include('_head', {}) %>

  <body>
    <main id="content" role="main" class="mt4">
      <article class="recipe-article">
        <%- include('_header', {}) %>
        <ul class="list-reset">
          <% posts.forEach((post) => { %>
          <li class="px3 mb4">
            <a href="./<%= post.filename %>">
              <h2 class="mb1"><%= post.postHeader.title %></h2>
            </a>
            <small><%= post.postHeader.date %></small>
            <p><%= post.postHeader.description %></p>
          </li>
          <% }); %>
        </ul>
      </article>
    </main>

    <%- include('_footer', {}) %>
  </body>
</html>
